import { PersianDatePipePipe } from './persian-date-pipe.pipe';

describe('PersianDatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new PersianDatePipePipe();
    expect(pipe).toBeTruthy();
  });
});
